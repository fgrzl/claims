package claims

const (
	// sub represents the subject claim name in JWT tokens.
	sub = "sub"
	// iss represents the issuer claim name in JWT tokens.
	iss = "iss"
	// aud represents the audience claim name in JWT tokens.
	aud = "aud"
	// exp represents the expiration time claim name in JWT tokens.
	exp = "exp"
	// nbf represents the not before claim name in JWT tokens.
	nbf = "nbf"
	// iat represents the issued at claim name in JWT tokens.
	iat = "iat"
	// jti represents the JWT ID claim name in JWT tokens.
	jti = "jti"
	// email represents the email claim name in JWT tokens.
	email = "email"
	// name represents the name claim name in JWT tokens.
	name = "name"
	// roles represents the roles claim name in JWT tokens.
	roles = "roles"
	// scope represents the scopes claim name in JWT tokens.
	scope = "scopes"
)
